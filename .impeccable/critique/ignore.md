# Designer-intended deviations — do not re-raise (see .impeccable/critique/*__apps-web-app.md)

# Each line cites the ruling that makes the deviation intentional. Unruled findings are NOT here.

# design.md "The three cards" itself specifies the DECISION card's left border ("Left border 3px amber");

# the shipped indigo-vs-amber question is D-1, owner-recorded, not an implementation agent's to settle.

border-left: 3px solid var(--indigo)

# The interrupt chip's urgency left rule is documented intent (globals.css "THE INTERRUPT CHIP (S1.4)"

# comment): urgency carried by a rule whose colour changes, legible without colour because the word is there.

.interrupt-chip

# The promotion inset rule is the documented §5.2 mechanism: the boundary between imported and native

# content is a visible EDGE (globals.css "A PROMOTION (S1.5)" comment). 2px, muted, never an accent.

.promotion-body

# Em dashes are the repo's established voice (design.md's own copy uses them; the composer placeholder

# is design.md "The composer" verbatim). Owner's voice outranks the skill's copy ban where they touch.

em-dash
