# Generated / machine-authored — not hand-formatted.
node_modules
dist
coverage
.next
pnpm-lock.yaml
# Local tooling config, written by the CLI (permission grants etc.), not by hand.
.claude/settings.local.json
# Impeccable critique snapshots — machine-persisted evidence artifacts (SHELL-B1), not hand-formatted.
.impeccable/
# A separate project that lives inside this working tree but is NOT part of this repo. Its files are
# not hand-formatted to this repo's config, and scanning them broke `pnpm verify` (and the pre-commit
# hook) for everyone. Excluded here; also gitignored.
packet-tracer-mcp/
# Office lock files (~$foo.docx) — transient binary temp files, not formattable.
~$*
