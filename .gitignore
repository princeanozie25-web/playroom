# Node
node_modules/
dist/
dist-test/
.next/
coverage/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
.venv/

# Env
.env
.env.*
!.env.example

# OS
.DS_Store

# Logs
*.log

# Local verification tooling (agent-browser) — never a repo dependency
agent-browser.json
*.har

# Demo capture output (scripts/demo-capture.ts). Videos belong in
# $PLAYROOM_CAPTURE_HOME, outside this tree; these rules exist so a stray run
# inside the repo cannot be committed.
videos/
*.webm
