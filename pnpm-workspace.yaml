packages:
  - 'apps/*'
  - 'packages/*'

# Native build scripts approved so `pnpm install` runs them and exits 0 on CI and
# fresh clones instead of blocking: esbuild backs vitest + tsx; sharp is Next.js's
# optional image-optimisation binary.
allowBuilds:
  esbuild: true
  sharp: true
